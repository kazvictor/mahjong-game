# Tests

Test directory for the Mahjong game.

Test runner and framework (e.g. Vitest) have not been installed yet — this
directory is intentionally reserved so test files have a stable home as the
project grows. Pure-logic modules in `src/core/` are written without side
effects so they can be unit-tested with minimal setup.
